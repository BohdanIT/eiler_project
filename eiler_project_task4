"""
Число-паліндром з обох сторін (справа ліворуч і ліворуч) читається однаково. Найбільше число-паліндром, 
отримане множенням двох двоцифрових чисел - 9009 = 91 × 99.
Знайдіть найбільший паліндром, отриманий множенням двох трицифрових чисел.
"""
p = []

for i in range(999, 99, -1):
    for x in range(999, 99, -1):
        if str(i * x) == str(i * x)[::-1]:
            p.append(i * x)
print(f'Найбільший паліндром - {max(p)}')
